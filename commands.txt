>git init [to initial]

>git status [to list]

>git add -A [to add]

>git commit -m "commit message"

>git clone

>git pull

>git push

>git config --list [to list configuration]

>git config --global user.name "username" [to add username]

>git config --global user.email "useremail"[to add useremail]